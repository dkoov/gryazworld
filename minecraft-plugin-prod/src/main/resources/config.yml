# Имя этого сервера (используется при регистрации порталов)
server-name: "survival"

# URL FastAPI-сервера
api-url: "http://155.212.210.252:8000"

# Сервер назначения по умолчанию (BungeeCord-имя)
target-server: "lobby"

# Секрет для аутентификации запросов к backend (X-Plugin-Secret)
plugin-secret: ""
